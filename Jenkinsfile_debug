pipeline {
    agent any

    environment {
        DOCKER_IMAGE = null // Initialiser la variable
    }

    stages {
        stage('Load Variables') {
            steps {
                script {
                    // Charger les variables depuis autre_variables.groovy dans le même répertoire
                    def loadedVariables = load 'variables.groovy'

                    // Définir la variable d'environnement pour une utilisation dans tous les blocs stage
                    DOCKER_IMAGE = loadedVariables.DOCKER_IMAGE
                    
                    //echo "Encore une autre variable : ${loadedVariables.AUTRE_VARIABLE}"
                    
                }
            }
        }
    stage('Checkout') {
            steps {
                checkout scm
            }
        }
    stage('Supprimer devops') {
            steps {
                script {
                    // Supprimez le repo devops Github
                    //sh 'rm -rf /home/kevin/git/devops'

                    def result = sh(script: 'rm -rf /home/kevin/git/devops', returnStatus: true)

                    if (result == 0) {
                        echo "Le répertoire a été supprimé avec succès."
                    } else {
                        echo "La suppression du répertoire a échoué."
                    }
                }
            }
        }
    stage('Get Dockerfile') {
            steps {
                script {
                    // Récupérez le Dockerfile
                    sh 'cd /home/kevin/git && git clone git@github.com:tkvy4/devops.git'
                }
            }
        }
    stage('Create path') {
            steps {
                script {
                    def path = '/home/kevin/docker_container/'
                    def docker = env.DOCKER_IMAGE
                    def final_path = path + docker
                    echo "Chemin final : ${final_path}"
                    
                    def directoryExists = sh(script: '[ -d ${final_path} ] && echo "true" || echo "false"', returnStdout: true).trim()

                    if (directoryExists == "true") {
                        echo "Le répertoire existe déjà. Continuer le build..."
                        sh 'cd ${final_path}'
                    } else {
                        echo "Le répertoire n'existe pas. Création..."
                        sh 'mkdir ${final_path} && cd ${final_path}'
                        echo "Répertoire crée."
                    }
                }
            }
        }
    stage('Build Docker Image') {
            steps {
                script {
                    sh 'cp /home/kevin/git/devops/Dockerfile ${final_path}'
                }
            }
        }
        stage('Run Docker Image') {
            steps {
                script {
                    sh 'docker run -d -p 8080:80 ${final_path}'
                }
            }
        }
    }
}
